#!/bin/sh

case "$(board_name 2>/dev/null)" in
xunlong,orangepi-cm5-base) ;;
*) exit 0 ;;
esac

[ -x /usr/sbin/usbmodeswitch-cm5-run ] || exit 0

logger -t usbmode-cm5 "starting boot-loop modeswitch retries"
/usr/sbin/usbmodeswitch-cm5-run boot-loop &

exit 0
